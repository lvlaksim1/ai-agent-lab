name: Agent Runtime Check

on:
  push:
    paths:
      - ".agent/**"
      - ".github/scripts/enqueue-agent-event.cjs"
      - ".github/scripts/stale-worker-recovery.cjs"
      - ".github/scripts/stale-worker-watchdog.cjs"
      - ".github/workflows/agent-stale-worker-recovery.yml"
      - ".github/workflows/agent-intake.yml"
      - "tools/validate-agent-runtime.mjs"
      - "tools/agent-report-contract.mjs"
      - "tests/agent-report-contract.test.mjs"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Check stale-worker recovery scripts syntax
        run: |
          node --check .github/scripts/stale-worker-recovery.cjs
          node --check .github/scripts/stale-worker-watchdog.cjs
      - name: Test report contract
        run: node tests/agent-report-contract.test.mjs
      - name: Validate agent runtime invariants
        run: node tools/validate-agent-runtime.mjs
