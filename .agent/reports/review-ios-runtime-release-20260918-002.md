# Приёмка смены ios-runtime-release-20260918-002

Вердикт: **CORRECTED**.

Предыдущая смена сработала по делу: диагностический commit `3111871` узкий, проверки не тронуты, Windows Build `35295897094` зелёный. E2E `35295897176` ожидаемо не прошёл boot-proof, зато выдал именно нужный artifact `10528396915` с трассой caller.

Новая трасса уже сузила участок: на `0xfffffff0070d7c1c` адрес ещё `X0=0xfffffef012ed0000`, а к `0xfffffff0070d7cf4` он превращается в аварийный `0x00003ef012ed0000`. Значит направление выбрано верно, но корневая причина пока не доказана.

Продолжение поправлено: следующей смене не надо повторно ждать или запускать тот же E2E. Надо сразу разобрать уже полученный участок `0x...0d7c1c..0x...0d7cf4`, найти точную операцию преобразования адреса и только после этого чинить причину.

До Definition of Done и релиза ещё не дошли.
