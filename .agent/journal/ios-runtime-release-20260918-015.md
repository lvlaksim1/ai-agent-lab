# ios-runtime-release-20260918-015 — technical journal

Worker: ivanych (Иваныч)
Proposed shift: 13
Target: lvlaksim1/iOS-Research-Runtime main

## Мой доклад

После независимой приёмки смены Борисыча взял exact b1295ce E2E artifact как базовую улику. Проверил qemu-debug.log напрямую: расширенный диапазон реально захватывает исполнение как минимум с 0x...0b25f8, но первое наблюдаемое 0x12ed0000 всё равно появляется только после инструкции 0x...0b3978 — до неё X2 остаётся pointer-like, а на следующем PC уже содержит 0x12ed0000. В добавленной upstream-странице более раннего carrier этого значения нет.

Корневую адресную семантику не менял. Сделал следующий минимальный диагностический шаг: расширил только первый caller/producer dfilter ещё на одну страницу назад, теперь с 0x...0b1000 до прежней верхней границы 0x...0b3b60. `tcg,one-insn-per-tb=on` и оба SPTM proof windows оставлены без изменений. Target commit cbd8783869c11857a8042cac834461051daf8031.

Сразу после push GitHub ещё не показывал workflow run для exact commit, поэтому CI/E2E результат этой сменой не выдумываю. Следующая независимая проверка должна дождаться exact cbd8783 Windows Build/E2E artifact и искать первую запись/инициализацию boot-state slot; если новое окно снова не покажет producer, нужно пересмотреть способ трассировки записей, а не подменять доказательство эвристическим PA-fix.
