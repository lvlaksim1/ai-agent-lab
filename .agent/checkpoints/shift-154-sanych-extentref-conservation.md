# Shift 154 — Саныч — DIR-029 extentref conservation checkpoint

- Immutable shift-154 Reporting v2 report commit `84515c38b5349055fb8a3d8b7f5c2b81bbceace4` passed exact `Agent Runtime Check` run `35646226276` SUCCESS.
- Re-consumed exact Windows E2E `35634992757` artifact `ios-darwin-windows-e2e` for target `3b0f5648f004f58daef526082b3d2a32d132edcf` and decoded every extentref child-leaf record using the pinned writer's exact physical-extent layout: key = physical start block in low 60 bits; value = `len_and_kind`, `owning_obj_id`, `refcnt`.
- Source: 719 records, 719 unique owners, every refcount=1, summed extent length = 46,740 blocks. APSB `TotalBlocksAllocated - TotalBlocksFreed = 49,426 - 2,686 = 46,740`; conservation is exact.
- Rebuilt: 1,360 records, 1,360 unique owners, every refcount=1, summed extent length = 51,651 blocks. APSB net allocation = 51,844 blocks, leaving exactly 193 blocks outside physical file extents.
- Pinned `go-apfs-v2/pkg/apfswrite` explains the rebuilt geometry directly: `physFiles()` emits one extentref record per non-empty stream/file; `fileDataBlocks` is the sum of those extents; `ownedBlocks = numFSTreeLeaves + numExtentrefLeaves + fileDataBlocks + snapBlocks`; APSB `FsAllocCount = 5 + ownedBlocks - snapshot-superblock-count`. Therefore the 193-block rebuilt delta is writer-owned metadata (fixed volume metadata plus FS-tree/extentref/snapshot structures), not evidence of missing physical extent records.
- Consequently the raw `719 vs 1360` extentref record-count divergence and rebuilt 193-block allocation delta do NOT justify APFS semantic mutation. They are consistent with source/rebuilt content/layout differences plus the writer's explicit one-physical-extent-per-nonempty-stream architecture.
- Next discriminator for mount error 79 must move away from extentref count/conservation. Highest-value read-only boundary is a fuller APSB/root-tree semantic comparison for fields not yet captured by `apfs-structural-evidence.json` (for example reserved/quota/unmount/history/formatted-by/modified-by and root file-extent semantics), before any writer change.
