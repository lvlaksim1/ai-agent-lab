# Shift 150 — DIR-029 extentref record discriminator

- DIR-033 control-plane repair is proven in this shift: lease-claim pulse kept `purpose=lease_claim`, processing heartbeat projected `activity_kind=starting`, and immutable Reporting v2 start report commit `909b034f13e0ea0f87ce0aa469bb3bb63d196091` passed exact `Agent Runtime Check` run `35633016653` SUCCESS.
- Consumed exact Windows End-to-End Boot run `35613817116` for target `a4897aa79186d0e492ca0e5f8fbdd0ad4338bcc9`: terminal FAILURE remains in `Run provisioning and Darwin root-shell proof`; exact artifact `ios-darwin-windows-e2e` id `10645357976` was downloaded and inspected.
- Artifact `apfs-structural-evidence.json` confirms both extentref roots are valid checksum-clean two-level index roots (`nodeFlags=1`, `nodeLevel=1`, subtype 15). Source has 7 index records; rebuilt has 13. Snapmeta remains an empty checksum-clean root leaf on both (`nodeFlags=3`, `nodeLevel=0`, `nodeNumberOfKeys=0`).
- The 7-vs-13 extentref root-record difference is now explained by the pinned `go-apfs-v2 v0.3.0` writer, not localized as corruption: `pkg/apfswrite/file.go` defines one physical-extent record per non-empty file/stream and grows extentref into an index-root plus leaves when those records overflow a leaf. The rebuilt image intentionally contains more files (1360 vs source 1261) because the sysroot is injected, so a different number of extentref leaves/index records is expected. Root index key/value bytes are therefore layout-dependent and are not evidence for copying source semantics.
- Boot log still gives no finer APFS diagnostic than repeated `apfs_vfsop_mount ... error 79` after `BSD root: md0`.
- No APFS semantic mutation has been made. Next actionable discriminator is read-only validation of the extentref child leaves referenced by the index root: verify child block addresses resolve, leaf headers/subtype/checksums are valid, and decode/validate physical-extent records/ranges/refcounts against the rebuilt allocation layout. Only a concrete invariant violation may justify a writer mutation.
