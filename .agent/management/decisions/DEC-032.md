# DEC-032 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
owner_decision_required: false
stop_production: false

## Assessment

Shift 57 produced no persisted target mutation, so the no-progress streak is now five shifts. However, it materially removed the remaining execution-route ambiguity required by DIR-014: the authoritative target path is `tools/ios-ramdisk-tool/main.go`, the complete file was retrieved without truncation (19,847 bytes), and the exact blob SHA `2aa440e1` was retained. Runtime loss occurred while preparing the full-file CAS wiring edit, before target mutation.

The present blocker is therefore not a missing architecture decision, missing external resource, or unresolved write mechanism. It is failure to persist the already-defined bounded mutation before runtime loss.

## Decision

Keep DIR-014 active without replacement. The next production shift must start from the proven full-file/blob-SHA CAS route and perform the localized NXSB wiring mutation first, then checkpoint immediately. It must not repeat architecture analysis, write-route discovery, or APFS-writer investigation before that attempt.

If the exact CAS update fails after the one authoritative reconcile permitted by DIR-014, persist the concrete failure and stop cleanly; that evidence will justify a new management intervention. Otherwise proceed to the known C# abort replacement and mandatory Windows gates/exact E2E.

No owner decision or production STOP is justified at this point. APFS writer remains frozen until causal structural evidence exists.
