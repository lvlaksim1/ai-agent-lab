# DEC-044 — CONTROL_PLANE_REMEDIATION

object_id: ios-research-runtime
decision: CONTROL_PLANE_REMEDIATION
health: RED
active_directive: DIR-018
owner_decision_required: false
stop_production: false

## Evidence

Shift 70 reached the mandatory immutable start-report barrier and its exact report commit `b2aa87d1675b8c22c7aeec579e43bfd488208453` failed Agent Runtime Check run 35466573056 at `Validate agent runtime invariants`. The committed report contains only unlabelled `ОЦЕНКА ПРЕДШЕСТВЕННИКА` and `МОЙ ПЛАН` sections and omits the literal v2 protocol fields required by `.agent/reporting.md` and the shared report contract (`Проект:`, `Работник:`, `Смена:`, `Начало смены:`, `СТАРТОВЫЙ ДОКЛАД:`, plus colon-terminated section labels). The target repository was not touched after the failed gate.

This is a control-plane/report-construction defect, not contradictory product evidence against DIR-017. Rewriting the immutable shift-70 report is forbidden.

## Decision

Supersede DIR-017 with DIR-018 for the next shift. Preserve the same mutation-first technical course, but add an explicit pre-publication requirement: construct the start report from the literal v2 template and validate all canonical markers before creating the immutable report commit. After the new report's exact Agent Runtime Check succeeds, immediately execute the already-localized bounded snapshot-preservation mutation and checkpoint the exact target SHA, then continue through focused tests, Windows gate and exact Windows E2E.

Do not change the APFS writer outside the bounded mutation without new causal structural evidence. No owner decision is required.