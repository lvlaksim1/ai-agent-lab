# DEC-081 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
active_directive: DIR-029

## Assessment

The temporary control-plane barrier in DIR-032 is no longer the active blocker. By shift 142, Reporting v2 / Agent Runtime Check gating was passing again and production had resumed the preserved DIR-029 APFS evidence chain.

Recent shifts produced verified narrowing rather than speculative mutation. Shift 142 independently confirmed the next bounded read-only discriminator behind already-green auxiliary-tree object headers/checksums: APFS B-tree node header fields `Flags`, `Level`, and `NumberOfKeys` for source/rebuilt extentref and snapmeta roots. The shift ended by verified runtime loss before instrumentation was implemented, not by an engineering dead end.

No owner decision, STOP, transfer, or architecture change is justified. Health remains ORANGE because the Windows-native release is still blocked on APFS mountroot error 79, but the causal search is converging and the next action is concrete.

## Decision

Close temporary DIR-032 as fulfilled and resume DIR-029 as the effective production directive. The next shift should continue from the durable shift-142 boundary: add bounded read-only node flags/level/key-count evidence for source/rebuilt extentref and snapmeta roots, run focused Go tests, Windows gate and exact Windows E2E, and consume the resulting discriminator before any APFS semantic mutation.
