# DEC-012 — KEEP_DECODED_APFS_EVIDENCE_COURSE

Object: `ios-research-runtime`
Decision: `KEEP_COURSE`
Health: `YELLOW`
Owner decision required: `false`

The last three production shifts produced verified narrowing rather than repeated speculative changes. Shift 31 attempted the authorized read-only APFS structural evidence channel; the resulting evidence showed that its source-side C# reader was attached to raw DMG bytes at the wrong image layer. Shift 32 then recovered the execution boundary and established the concrete safe implementation point: source NXSB must be captured through `disk.OpenWithOffset(opts.input)` and rebuilt NXSB from the bare staging image before DMG wrapping.

No APFS writer semantic change is justified yet. Keep DIR-011 active. The next production execution should implement only this decoded-layer evidence path, rerun the existing gates and exact Windows E2E, and use the resulting source-vs-rebuilt structural comparison to identify the first causally relevant incompatibility before any writer change.

The runtime loss of shift 32 is an orchestration/liveness event, not evidence that the technical course is wrong. Production is idle and pending independent OTK review/recovery; management must not replace that review or start a worker.