# DEC-069 — KEEP_COURSE; EXECUTE DIR-027 WITHOUT RE-DISCOVERY

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
active_directive: DIR-027
owner_decision_required: false
stop_production: false

## Assessment

Production is idle after shift 113. Two consecutive scored shifts produced no engineering progress because runtime was lost after the Reporting v2 barrier/preimage verification and before the already-defined bounded KeyOSVersion repair. This is costly repetition, but it does not invalidate the technical direction: DIR-027 already defines a narrow, evidence-backed mutation boundary and forbids broad APFS re-investigation.

The target remains unchanged at the verified pre-repair state. There is no transfer request, no owner decision is required, and no evidence justifies weakening the Definition of Done or stopping new production shifts.

## Decision

KEEP_COURSE. DIR-027 remains the sole active directive. The next production shift should consume the existing verified lossless-preimage evidence, revalidate the production fence as required, perform the bounded KeyOSVersion repair, checkpoint the exact target commit, then run focused tests -> Windows gate -> exact Windows E2E and consume terminal evidence in the same live shift.

Do not spend another shift rediscovering the already-established APFS repair boundary unless authoritative target state has changed or the existing evidence fails revalidation.

## Management conclusion

Health remains ORANGE because repeated runtime loss has consumed shifts without moving the target. The engineering course itself remains supported; the management correction is execution discipline, not a new architecture or scope change.