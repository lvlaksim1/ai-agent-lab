# DEC-060 — CHANGE_COURSE

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE
active_directive: DIR-024
owner_decision_required: false
stop_production: false

## Assessment

Three shifts have elapsed since the previous manager review. Shift 96 produced the decisive new result: in the pinned go-apfs-v2 path, `FixedTime` reaches `builder.timestamp` / `FormattedBy.Timestamp` but does not assign APSB `LastModTime` (reader `ModificationTime`). Shift 97 then suffered verified runtime loss before producing a durable checksum/serialization mechanism. OTK approved shift 97 at 5/10 with progress_class=none.

That evidence invalidates the executable assumption inside DIR-023 that the next bounded product action can simply be `modificationTime -> existing FixedTime` through the already-localized primitive. Repeating deterministic whole-file reconstruction toward that same ineffective primitive would spend another shift on a mutation now known not to write the target field.

## Decision

CHANGE_COURSE narrowly. Preserve the APSB LastModTime hypothesis, but replace the obsolete mutation instruction with a mechanism-first proof boundary: next production must identify the smallest checksum/serialization-safe way to assign APSB LastModTime in the pinned writer path, prove where checksum regeneration occurs, and only then make one bounded mutation. Do not reopen XID/checkpoint or MetaCrypto without new discriminating evidence.

No owner decision or STOP is required. Production may resume automatically under DIR-024.