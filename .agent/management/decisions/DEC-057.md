# DEC-057 — CHANGE_COURSE

object_id: ios-research-runtime
decision: CHANGE_COURSE
created_at: 2026-09-20T08:12:25Z
trigger: consecutive_no_progress

## Assessment

Production is idle and shift 91 is pending independent OTK after verified runtime loss. The latest durable checkpoint shows the worker passed the report gate and re-read the exact FixedTime target boundary, but did not mutate the target because the returned whole-file content was truncated. This is now a repeated execution-path blocker, not evidence that the APSB hypothesis itself is wrong.

The object remains ORANGE. The bounded APSB `modificationTime -> existing FixedTime` preservation remains the nearest evidence-backed product step, but repeatedly attempting it through a single truncated whole-file response is wasteful.

## Decision

Change the execution method, not the technical hypothesis. Issue DIR-023 for the next production shift after independent OTK: reconstruct the authoritative target file deterministically from bounded GitHub reads/chunks, verify the reconstructed preimage against the authoritative blob SHA, apply only the already-localized FixedTime preservation edit, and perform a whole-file CAS-safe write. If exact preimage reconstruction or SHA verification cannot be established, do not write and persist the concrete retrieval defect as evidence.

Do not reopen XID/checkpoint or speculative MetaCrypto work without new evidence. Preserve the existing verification chain: exact target SHA checkpoint, focused tests, Windows gate, exact Windows E2E.

No owner decision, STOP, or transfer is required.
