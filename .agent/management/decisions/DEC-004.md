# DEC-004 — KEEP_COURSE

Object: ios-research-runtime
Decision: KEEP_COURSE
Health: YELLOW

## Management assessment

Production continues to make verified incremental progress toward the boot blocker without weakening proof gates or changing guest address semantics. Shift 16 established an important boundary: the boot-state slot VA cannot be converted honestly to a constant PA without runtime SPTM `physlo/virtlo` evidence, and it identified the TCG store/TLB layer as the next narrow diagnostic location.

The immediate pending event is the independent OTK review of shift 16. Production is idle, so there is no concurrency hazard. No transfer is requested, no repeated no-progress pattern is present, and no owner decision is required.

## Course

Keep the current boot-debugging strategy. Let OTK independently validate shift 16. If accepted, the next production step should first expose/verify the runtime SPTM mapping and derive the slot PA from that evidence, then use that proven mapping for a narrowly scoped TCG-store diagnostic probe and run the pinned Windows gate plus exact E2E. Do not introduce a speculative hard-coded PA or address-semantics fix before the probe evidence.

No directive is issued; the existing production continuation is sufficiently precise.
