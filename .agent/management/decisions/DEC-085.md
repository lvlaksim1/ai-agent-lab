# DEC-085 — KEEP DIR-033 ON THE LOCALIZED PRODUCER DEFECT

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
production_liveness: NO_WORKER
owner_decision_required: false
stop_production: false

## Assessment

Shift 149 materially narrowed the recurrent Reporting v2 barrier. Its immutable start report is canonical; the exact Agent Runtime Check `35626501531` failed because the lease-claim producer projected `heartbeat.activity_kind: lease_claim` into processing state. The liveness contract permits `starting` at this stage, while `lease_claim` is a pulse purpose rather than a legal processing activity kind.

This is concrete incremental progress under DIR-033, not evidence that another course change is needed. Production is idle/NO_WORKER, target `lvlaksim1/iOS-Research-Runtime` remains unchanged at the preserved APFS boundary, and no owner decision or transfer is required.

## Decision

KEEP_COURSE. DIR-033 remains active without expansion: the next shift must repair only the demonstrated lease-claim -> processing-state projection so the heartbeat starts as `activity_kind: starting`, preserve all existing invariants, and prove a fresh canonical immutable Reporting v2 start report with exact Agent Runtime Check SUCCESS. Only after that green gate may production resume DIR-029 from target `a4897aa79186d0e492ca0e5f8fbdd0ad4338bcc9` and the established extentref root-record `NumberOfKeys` 7-vs-13 discriminator.

No owner decision is required. No transfer is required. Scheduled Tasks and scheduler topology remain untouched.
