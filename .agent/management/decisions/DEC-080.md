# DEC-080 — CHANGE_COURSE; RESTORE START-REPORT RUNTIME BARRIER BEFORE APFS WORK

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE
reviewed_shift: 139

## Assessment

Production is idle, no transfer is pending, and manager attention was raised both by the normal three-shift cadence and by `START_REPORT_RUNTIME_CHECK_FAILURE_SHIFT_139`.

Shift 139 never reached target work. Its immutable start report was published and bound into runtime state, but the mandatory `Agent Runtime Check` run `35587838815` for commit `29b993565a0bf08fc346ab51daed56ef7d1f3005` failed specifically at `Validate agent runtime invariants`. The failing commit only bound `shift_start_report_path` / `shift_start_report_commit` and changed heartbeat activity detail; therefore this is a control-plane/report-barrier failure, not new APFS evidence.

This recurs after the earlier DIR-031 incident, so simply keeping DIR-029 as the immediate next action would waste another production shift. The technical APFS continuation itself remains valid: target state still records commit `735c8e4d5ae8187ff813b57a36c664c1b015085f` and exact Windows E2E `35583468605` as the terminal FAILURE awaiting consumption.

## Decision

1. Temporarily preempt DIR-029 with DIR-032 for the next shift.
2. Treat run `35587838815` and commit `29b993565a0bf08fc346ab51daed56ef7d1f3005` as the authoritative failing case; determine the exact rejected invariant rather than inferring it from earlier incidents.
3. Repair only the demonstrated producer/validator/control-plane mismatch. Do not weaken validation, fencing, GitHub time authority, stale recovery, Reporting v2, OTK, scheduler immutability, or proof gates.
4. Require an authoritative successful Agent Runtime Check before target-repository work resumes.
5. After the barrier is proven green, return directly to DIR-029 at the preserved exact E2E/APFS evidence boundary; do not reopen closed hypotheses.
6. Keep health ORANGE. No owner decision, STOP, or transfer is required.
