# DEC-079 — KEEP_COURSE; TWO NO-PROGRESS SHIFTS DO NOT INVALIDATE DIR-029

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
reviewed_shift: 136

## Assessment

Production is idle and no transfer is pending. Manager attention was raised because shifts 135 and 136 were both classified `progress_class=none`.

The two no-progress outcomes do not show that the technical course is exhausted. Both shifts ended by independently verified runtime loss before the bounded DIR-029 evidence step produced a durable discriminator. Shift 136 preserved the correct evidence-first boundary, passed its mandatory start-report Runtime Check, and made no speculative APFS or packaging mutation.

The target repository remains at `b75810a9ede0557205bd5948313d9687e4fdeca5`; therefore there is no newer target change that invalidates the current continuation. The highest-value unresolved action remains the already narrowed read-only `extentrefTreeOid` / `snapMetaTreeOid` OMAP → physical block → object header/checksum comparison.

Repeated runtime loss is operationally costly, but the current evidence does not establish a new control-plane invariant defect or justify reopening closed APFS hypotheses. The next worker should persist the first useful tree-resolution result as soon as it is obtained so a later runtime loss cannot erase that discriminator.

## Decision

1. Keep DIR-029 active; do not broaden the APFS search and do not authorize semantic mutation without a concrete mismatch.
2. Resume exactly from the shift-136 continuation boundary: live-volume `extentrefTreeOid` and `snapMetaTreeOid` → OMAP → physical blocks → OID/XID/type/subtype/Fletcher comparison.
3. Require early durable persistence of the first new discriminator/evidence before proceeding deeper into the chain; this is an execution-focus instruction, not a relaxation of proof gates.
4. Keep health ORANGE. The boot blocker and release Definition of Done remain unresolved, but there is still a concrete bounded diagnostic path.
5. No owner decision, STOP, transfer, or new directive is required.
