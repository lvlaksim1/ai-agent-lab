# DEC-086 — CHANGE_COURSE

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE

## Evidence

Production is idle (NO_WORKER). Shift 152 ended after runtime loss at the mandatory report barrier and its exact Agent Runtime Check `35638258123` failed at `Validate agent runtime invariants`.

The authoritative job log localizes the failure completely: immutable start report `.agent/reports/starts/shift-152-palych-ios-runtime-release-20260918-032.md` is missing every canonical Reporting v2 literal marker checked by the validator: `Проект:`, `Работник:`, `Смена:`, `Начало смены:`, `СТАРТОВЫЙ ДОКЛАД:`, `ОЦЕНКА ПРЕДШЕСТВЕННИКА:`, and `МОЙ ПЛАН:`.

This is not new APFS evidence and not a reason to weaken the validator. It is a recurrence/bypass of the start-report producer contract after prior control-plane repairs. Target mutation did not occur in shift 152.

## Decision

CHANGE_COURSE at the control-plane barrier. Replace the bounded control-plane objective with DIR-034: diagnose why shift 152 again bypassed the canonical Reporting v2 renderer/producer path, repair only the demonstrated producer/control-plane defect, and require a fresh exact Agent Runtime Check SUCCESS before target mutation.

After that gate is green, resume the preserved DIR-029 engineering boundary: consume terminal Windows E2E `35634992757` and continue extentref child-leaf comparison. Do not restart broad APFS investigation.

No owner decision, transfer, STOP, rating change, or scheduler mutation is required.
