# DEC-019 — KEEP_COURSE_PENDING_SHIFT_43_OTK

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
production_liveness: NO_WORKER
owner_decision_required: false
stop_production: false

## Assessment

Production is idle after confirmed runtime loss of shift 43. An independent supervisor-review event for shift 43 is already pending, so the production chain is correctly waiting for OTK rather than starting another worker.

The technical direction remains evidence-first. The decoded NXSB reader/serializer path is already localized; the missing work is still the read-only source/rebuilt snapshot wiring followed by the existing Windows gates and exact E2E. No evidence justifies changing APFS writer semantics yet.

Two consecutive no-progress classifications are explained by runtime losses rather than a demonstrated technical dead end. DIR-011 remains the correct constraint and does not need replacement.

## Decision

Keep the current course. Allow independent OTK to process shift 43, then let the normal relay start the next production worker. The next worker must continue DIR-011: wire the decoded source/rebuilt NXSB evidence path, run the existing Windows gates/exact E2E, and only modify APFS writer fields after a causally relevant incompatibility is demonstrated.

No owner decision is required. No new directive is issued.
