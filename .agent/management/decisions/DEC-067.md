# DEC-067 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
active_directive: DIR-027
reason: TWO_NO_PROGRESS_SHIFTS

## Assessment

Production is idle, no transfer is requested, and shift 109 has been independently finalized APPROVED with progress class `none`. The management counter now records three consecutive no-progress shifts, but the evidence still does not establish a technical dead end in DIR-027: the bounded KeyOSVersion repair itself has not yet been executed. Shift 109 ended by verified runtime loss after the Reporting v2 gate while acquiring the already-known authoritative `main.go` preimage and before target mutation.

The repeated cost is runtime continuity, not a contradictory APFS result. Broadening the writer now would discard the strongest causal hypothesis without testing it.

## Decision

Keep DIR-027 active unchanged. The next live production shift should minimize pre-mutation work: re-establish the required authoritative lossless preimage and fence, then proceed directly to the single bounded source-preserving APSB KeyOSVersion offset-108 repair with Fletcher64/checksum validation. After the exact target SHA is checkpointed, run focused tests, Windows gate and exact Windows E2E and consume terminal evidence while the worker remains live.

Do not reopen LastModTime, XID/checkpoint, adjacent MetaCrypto fields, or broad APFS investigation unless the bounded repair produces new discriminating evidence. No owner decision, STOP, transfer, or architecture change is justified.