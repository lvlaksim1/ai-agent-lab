# DEC-008 — EVIDENCE_ACCESS_ESCALATION

Object: ios-research-runtime
Decision: BLOCKED / OWNER_DECISION_REQUIRED
Date: 2026-09-18T19:33:20Z

The project has verified progress through SPTM/XNU to `BSD root: md0`, but rebuilt recovery APFS still fails `mountroot` with error 79. OTK independently confirmed that the last production shift correctly avoided speculative APFS writer changes and that the missing evidence is the exact E2E artifact/full job log or an equivalent extracted structural NX/APFS/checkpoint dump.

The current production runtime cannot obtain the binary artifact/full job log needed to identify the first incompatible metadata field. Repeating the same production action would not create new evidence and is therefore not authorized.

Management course:
- preserve all existing proof gates and the current APFS writer;
- do not guess or patch NX/APFS/checkpoint semantics without causal evidence;
- keep the object BLOCKED until an evidence channel is available;
- owner decision is required to provide/authorize an accessible exact E2E artifact/full log or equivalent compact structural source-vs-rebuilt dump that the production runtime can read.

No transfer is requested. Production is not emergency-stopped; it is evidence-blocked with no eligible continuation.