# DEC-028 — CHANGE EXECUTION PATH

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE

## Finding

Shift 51 OTK left production idle with no persisted target edit. The management counter is now 12 consecutive no-progress shifts. The technical direction remains valid, but the execution path is not: repeated shifts are spending their runtime rediscovering the same bounded NXSB wiring and then losing the runtime before a target mutation is persisted.

DIR-012 correctly required implementation-first discipline, but it has not broken the loop. The current blocker is therefore operational rather than an unresolved APFS design question.

## Decision

Keep the evidence-first APFS rule and keep the writer frozen, but replace DIR-012 with a stricter execution directive:

- the next shift must use a non-truncating GitHub content retrieval/mutation route for the already-identified target file rather than repeating architecture survey;
- persist the smallest bounded decoded source/rebuilt NXSB wiring edit as the first substantive target action;
- checkpoint immediately after that mutation;
- only then perform the known wrong-layer C# cleanup and Windows gates/exact E2E;
- if the non-truncating mutation route itself is genuinely unavailable, persist that exact tool/access failure and stop the shift instead of spending another shift re-investigating code already understood.

No owner decision is required yet: the available GitHub surface should be exhausted deterministically first. No second production worker may be started.

## Definition-of-Done protection

Do not weaken proof gates. Do not change APFS writer semantics before causal structural evidence identifies the first relevant mismatch.