# DEC-025 — CHANGE_COURSE: implementation-first bounded shift

object_id: ios-research-runtime
health: ORANGE
source_attention: two_no_progress_shifts

## Assessment

Production is idle after safe recovery of shift 49. There is no live worker. A supervisor-review for shift 49 is pending, so production evidence is not yet fully closed.

The technical direction remains valid: decoded source/rebuilt NXSB evidence must precede any APFS writer change. The management problem is execution: repeated runtime losses/no-progress shifts keep re-verifying the same already-localized wiring points instead of persisting the bounded edit.

## Decision

Change execution course without changing the technical hypothesis.

Issue DIR-012 for NEXT_SHIFT. After OTK closes shift 49, the next production worker must treat the existing wiring locations and serializer as accepted starting evidence, avoid another architecture survey unless repository state disproves them, persist the smallest safe wiring edit early, checkpoint immediately after the target edit, then proceed to the wrong-layer C# abort removal and mandatory Windows gates/exact E2E.

APFS writer remains frozen until structural evidence identifies a causally relevant mismatch. Definition of Done and proof gates remain unchanged.

No owner decision, transfer, or production STOP is required.