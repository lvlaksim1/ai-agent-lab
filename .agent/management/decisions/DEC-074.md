# DEC-074 — CHANGE_COURSE: REPAIR CONTROL-PLANE RUNTIME CHECK BEFORE PRODUCTION

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE
source_attention_generation: 67

## Evidence

Shift 124 published its immutable Reporting v2 start report at commit `5b8120358e51610f53328c94fd6780bce8b2ddb5`. Exact `Agent Runtime Check` run `35554810708` failed in `Validate agent runtime invariants` before target work.

At that exact triggering commit, production state used `heartbeat.activity_kind = "lease_claim"`. The current authoritative validator accepts only `starting`, `working`, `external_wait`, `persisting`, `closing`, `otk_review`, and `blocked_control_plane`. Therefore the control plane can emit a production-claim state that its own mandatory runtime gate rejects.

Stale recovery subsequently fenced shift 124 and returned production to idle. This is a control-plane contract mismatch, not new APFS evidence and not a reason to weaken any invariant.

## Decision

Do not continue iOS target work until the producer/validator activity-kind contract is made internally consistent and the mandatory Agent Runtime Check passes on the repaired control plane. Preserve all scheduler, liveness, fencing, Reporting v2, OTK, and production-concurrency invariants. After proof of a successful runtime check, resume the existing DIR-029 technical course without broadening APFS mutations.

Owner decision is not required: prior authorization covers repairing the control-plane Agent Runtime Check without weakening invariants and resuming production after successful verification.
