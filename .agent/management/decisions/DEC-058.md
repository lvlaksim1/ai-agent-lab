# DEC-058 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
active_directive: DIR-023
owner_decision_required: false
stop_production: false

## Assessment

Production is idle and there is no active transfer. The latest scored shift is 91, OTK-approved, but it produced no target mutation and management counters now show six consecutive no-progress shifts. The current wake reason is TWO_NO_PROGRESS_SHIFTS.

DIR-023 is nevertheless still the correct bounded recovery course and has not yet been exhausted. It explicitly forbids dependence on a single whole-file response and requires deterministic non-overlapping bounded reads (or exact Git blob representation), exact preimage/blob verification, then only the localized APSB `modificationTime -> existing FixedTime` CAS mutation. Shift 91 stopped because returned whole-file content was truncated after re-reading the localized boundary; that is not evidence that the DIR-023 bounded reconstruction method is impossible.

## Decision

KEEP_COURSE. Do not reopen APFS/XID/MetaCrypto discovery and do not create a new technical directive. The next production shift must execute DIR-023 literally: reconstruct the authoritative preimage from bounded non-overlapping reads, verify it against the current blob SHA, and only then perform the localized CAS-safe mutation. If exact reconstruction or SHA verification truly cannot be established with the available GitHub primitives, persist the concrete primitive-level defect once, without target mutation, as DIR-023 already requires.

No owner decision is required. Production may resume automatically from the existing pending wake.