# DEC-068 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
active_directive: DIR-027
owner_decision_required: false
stop_production: false

## Evidence

Shift 110 was finalized CORRECTED with progress_class=none after a Reporting v2 start-report contract failure and verified runtime loss before target mutation. This extends the no-progress streak to four, but it does not falsify DIR-027: the bounded KeyOSVersion repair still has not been executed. The prior lossless-preimage problem is already resolved, and the remaining repair boundary is narrow, source-preserving, checksum-safe, and testable.

## Decision

Keep DIR-027 unchanged. Do not broaden APFS investigation and do not alter LastModTime, XID/checkpoint, adjacent MetaCrypto fields, or unrelated code. The next production shift must first satisfy the Reporting v2 start barrier exactly, then minimize pre-mutation overhead, revalidate authoritative preimage and fence, execute the single offset-108 KeyOSVersion repair with Fletcher64/checksum validation, checkpoint the exact target SHA, and consume focused tests → Windows gate → exact Windows E2E terminal evidence in the same live shift.

The repeated runtime losses are an execution/reliability cost, not yet evidence that the technical course is wrong. Escalate course again only if the bounded repair is actually executed and fails to produce discriminating evidence, or if a new integrity/owner-goal conflict appears.

No owner decision, STOP, transfer, or new directive is required.
