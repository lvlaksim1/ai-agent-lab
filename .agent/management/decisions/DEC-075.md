# DEC-075 — KEEP_COURSE: EXECUTE NARROW DIR-029 APFS EVIDENCE INSTRUMENTATION

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
source_attention_generation: 68

## Evidence

The control-plane gate from DEC-074/DIR-030 is no longer the active blocker: management state has returned the active directive to DIR-029 and OTK accepted shift 126 as APPROVED with substantial progress.

Shift 126 exhausted the immediate exact-run/job/log route for exact Windows E2E `35551527247` far enough to justify the next step already permitted by DIR-029: the narrowest read-only CI/APFS instrumentation that can expose the first API-visible live-volume object, lookup, validation, checksum, XID or equivalent discriminator before mountroot returns error 79.

No evidence currently justifies another APFS semantic mutation, packaging correction, XID/checkpoint experiment, KeyOSVersion experiment, adjacent MetaCrypto mutation, or relaxation of any proof gate.

## Decision

Keep DIR-029 unchanged. The next production shift should implement/use only bounded read-only diagnostic instrumentation necessary to expose the first concrete APFS failure boundary, consume the resulting authoritative Windows evidence in the same live shift where practical, and tie any later mutation directly to that discriminator.

No owner decision, STOP, transfer, or new management directive is required.
