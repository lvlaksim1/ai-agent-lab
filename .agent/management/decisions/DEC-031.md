# DEC-031 — CHANGE_COURSE

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE
owner_decision_required: false
stop_production: false

## Assessment

DIR-013 corrected the strategy, but shifts 53–56 still produced no persisted target mutation. Shift 56 again stopped at searching for a safe non-truncating write route even though the required mutation boundary is already settled. The blocker is now execution-route ambiguity, not APFS architecture.

## Decision

Issue DIR-014 with an explicit repository-contents CAS route: retrieve the exact current `main.go` revision without truncation, replace only the already-localized NXSB wiring, write it back against the exact blob SHA, and checkpoint immediately. No further survey of alternative write mechanisms is allowed before this mutation attempt.

If that exact route fails, persist the concrete API/tool error and stop cleanly for management escalation. Keep the APFS writer frozen until causal structural evidence exists. Windows gates and exact E2E remain mandatory after the evidence wiring is persisted.
