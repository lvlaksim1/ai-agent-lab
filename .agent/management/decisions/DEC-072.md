# DEC-072 — CORRECT_COURSE

object_id: ios-research-runtime
health: ORANGE
decision: CORRECT_COURSE
supersedes_course: DIR-028

## Evidence

Shift 120 OTK independently confirmed that exact Windows E2E `35545935392` disproved the suspected ramdisk packaging-size divergence: `patched_dmg_bytes=192298899`, while md0 capacity is `192299008` bytes, exactly the image rounded to 512-byte sectors. Boot still reaches `BSD root: md0` and `apfs_vfsop_mountroot` repeatedly returns error 79. The shift changed only read-only size instrumentation and preserved the established APFS constraints.

## Decision

DIR-028 has reached its success boundary with a negative packaging discriminator. Do not continue packaging work and do not guess another APFS mutation. Resume APFS investigation only through a read-only mountroot evidence channel that localizes the first failing APFS object/invariant behind error 79. Mutation is permitted only after that evidence identifies a bounded causal field or boundary.

Issue DIR-029 for the next shift. Production may resume automatically; no owner decision is required.
