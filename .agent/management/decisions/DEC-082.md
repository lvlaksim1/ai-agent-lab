# DEC-082 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
active_directive: DIR-029

## Assessment

Manager attention was raised by two consecutive no-progress shifts after DEC-081. This is not evidence that the DIR-029 technical route is exhausted. Shift 144 independently shows that the immutable Reporting v2 start report and exact Agent Runtime Check succeeded, the worker inherited the bounded read-only B-tree-node discriminator correctly, and runtime was then lost before any target mutation or engineering evidence could be produced.

The current blocker therefore remains unchanged: APFS mountroot error 79 is narrowed behind already-green auxiliary-tree object headers/checksums, while `Flags`, `Level`, and `NumberOfKeys` for source/rebuilt extentref and snapmeta roots remain the nearest untested read-only discriminator. Repeated runtime loss has cost productive shifts, but there is no evidence-backed basis for broadening APFS mutations, changing architecture, weakening gates, or requesting an owner decision.

## Decision

KEEP_COURSE under DIR-029. The next production shift should execute the already-defined bounded read-only B-tree node `Flags`/`Level`/`NumberOfKeys` comparison for source/rebuilt extentref and snapmeta roots, then run focused Go tests, Windows gate and exact Windows E2E and consume the first concrete discriminator before any APFS semantic mutation.

If another live shift reaches the same boundary but cannot obtain this discriminator for a technical reason (rather than runtime loss), return the exhausted evidence route to management instead of repeating the same attempt or speculating.
