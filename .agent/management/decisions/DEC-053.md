# DEC-053 — KEEP_COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE

## Assessment

Since DEC-052, production made verified causal progress. Shift 83 repaired the exact E2E APSB evidence path; run 35486388059 on target b5d83ec reached the same APFS mountroot error 79 while producing current source/rebuilt volume-superblock evidence. Core feature/tree/UUID/role fields match. The remaining discriminating differences recorded in authoritative object state include `metaCryptoKeyOsVersion` (source 407249186 vs rebuilt 0) and `modificationTime` (source nonzero vs rebuilt 0).

Shift 84 then lost runtime before target work. Its independent OTK verdict is APPROVED, so that runtime loss is not evidence that the technical direction failed. Production is currently idle and there is no transfer or owner-decision requirement.

## Decision

Keep the evidence-first boot-debugging course. The next production shift should analyze the pinned APFS writer mapping for `metaCryptoKeyOsVersion` and `modificationTime` against the newly captured source/rebuilt APSB evidence before any semantic mutation. Only a demonstrated mapping/semantic defect may justify the nearest bounded writer change, followed by the existing Windows gates and exact E2E.

Do not reopen settled snapshot-preservation or checkpoint-XID work. Preserve DIR-022's reporting/runtime invariants and no-speculation constraints. No owner decision or production STOP is required.
