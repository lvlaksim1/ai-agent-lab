# DEC-014 — KEEP DECODED APFS EVIDENCE COURSE

object_id: ios-research-runtime
decision: KEEP_COURSE
health: YELLOW
production_liveness: NO_WORKER

## Assessment

The object remains in boot-debugging and has not reached the mission Definition of Done. Production is currently idle with a pending continuation; there is no active worker to interrupt.

The last three scored shifts produced incremental verified progress rather than repeated no-progress: the investigation separated the APFS diagnostic-reader failure from the production writer path and narrowed the next evidence point to the decoded partition-relative layer used by `ios-ramdisk-tool`.

The current blocker is therefore actionable and internal: implement read-only source/rebuilt NXSB snapshots at the decoded layer, route compact evidence through the exact Windows E2E path, run mandatory gates, and only then consider a writer-semantic change if a causally incompatible field is proven.

## Decision

KEEP_COURSE.

DIR-011 remains active and sufficient. Do not create a new directive. Do not weaken proof gates, do not guess APFS semantics, and do not modify APFS writer fields before decoded-layer evidence identifies a concrete causal incompatibility.

No owner decision is required. Production may continue from the existing pending continuation on the next production relay tick.