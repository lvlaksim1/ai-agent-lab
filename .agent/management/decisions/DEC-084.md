# DEC-084 — RESTORE RECURRENT REPORTING V2 BARRIER

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: ORANGE
production_liveness: NO_WORKER
owner_decision_required: false
stop_production: false

## Assessment

Shift 148 made no target progress because its immutable Reporting v2 start report omitted the canonical literal metadata/marker lines. Exact Agent Runtime Check `35620263457` then failed at `Validate agent runtime invariants`, and the worker correctly kept target mutation forbidden.

This is a recurrence of the control-plane defect class previously handled by DIR-032, after DIR-032 had been closed on evidence that the barrier was passing by shift 142. The correct response is therefore not to broaden APFS work or weaken the validator, but to reopen a bounded control-plane repair against the exact shift-148 failing case.

Production is currently idle/NO_WORKER. The active object remains iOS-Research-Runtime, and DIR-029's APFS evidence chain must be preserved rather than restarted.

## Decision

Issue DIR-033 for the next shift. It must identify why the canonical Reporting v2 producer path regressed or was bypassed in shift 148, repair only that demonstrated producer/validator contract defect, and prove the exact Agent Runtime Check green before any target-repository mutation. After the barrier is restored, resume DIR-029 from the preserved extentref root-record comparison boundary at target `a4897aa79186d0e492ca0e5f8fbdd0ad4338bcc9`.

No owner decision is required. No transfer is required. Scheduler topology and Scheduled Tasks remain untouched.