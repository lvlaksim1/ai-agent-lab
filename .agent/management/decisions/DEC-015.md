# DEC-015 — KEEP COURSE, TREAT RUNTIME LOSS AS EXECUTION RISK

object_id: ios-research-runtime
decision: KEEP_COURSE
health: ORANGE
production_liveness: NO_WORKER

## Assessment

Production is idle after a second consecutive runtime-loss/no-progress shift. The latest worker did not regress the technical direction: shift 36 explicitly moved from repeated inspection toward implementing the already-directed decoded-layer read-only NXSB evidence channel, but the runtime disappeared before that implementation was persisted.

The technical blocker remains actionable and internal. DIR-011 still describes the correct causal path: collect source/rebuilt decoded APFS structural evidence first, then change writer semantics only if a concrete incompatibility is proven.

The repeated runtime losses are now a production-execution risk rather than evidence that the APFS investigation itself is wrong. Health is raised from YELLOW to ORANGE so another no-progress/runtime-loss cycle is not treated as routine.

## Decision

KEEP_COURSE.

DIR-011 remains active and sufficient; do not create a new technical directive. The next production worker should implement and persist the smallest useful decoded NXSB snapshot/evidence increment early, before longer gates or external waits, then continue through mandatory gates and exact Windows E2E if runtime permits.

Do not weaken proof gates. Do not guess APFS semantics. Do not modify APFS writer fields before causal decoded-layer evidence exists.

No owner decision is required at this point. If the next shift again ends in runtime loss without a persisted evidence increment, escalate the execution/reliability problem rather than sending another worker through the same unpersisted loop.