# DEC-033 — CONTROL-PLANE GATE DEFECT

object_id: ios-research-runtime
decision: STOP_AND_REPAIR_CONTROL_PLANE
health: RED

## Evidence

Shift 59 never reached target-repository work. Its lease-claim commit `7f5c7e7e75857db78e62589ba1baefd8a8bd2557` already failed the `Agent Runtime Check` validation job, before the immutable start report was published. The start-report commit `ef24e9bda8b72a8a0243746c497d218b5092cf86` then failed the same runtime invariant validation. The report body itself contains every literal v2 start-report marker required by `tools/agent-report-contract.mjs`, so treating this as a worker report-format error is not supported by the evidence.

The previous idle OTK-finalized state at `18c33ce77ab6abbe3feddabc9befa008dc09d414` passed `Agent Runtime Check`. Therefore the regression boundary is the transition from the valid idle state into the shift-59 processing claim. Production recovery later fenced the stale execution without touching the target repository.

## Decision

Stop new production shifts until the control-plane invariant failure on a normal production lease claim is diagnosed and repaired. Do not alter the iOS/APFS technical course: the verified next product step remains exact Windows E2E on the decoded source/rebuilt NXSB evidence path under DIR-014.

This is an orchestration/control-plane blocker, not evidence against the current APFS direction. Do not rewrite the immutable shift-59 start report and do not bypass or weaken `Agent Runtime Check`.

Owner action is required because the current production protocol forbids target work until this gate passes, while the production worker cannot legally cross the failed gate to repair the orchestration layer from inside a shift.