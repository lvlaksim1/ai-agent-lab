# DEC-051 — CONTROL_PLANE_REPAIR: normalize production-start activity kind

object_id: ios-research-runtime
decision: CHANGE_COURSE
health: RED

## Evidence

Shift 79 published an immutable Reporting v2 start report with the required canonical literal labels. The exact report commit `df0c791a1bafa518946f3c44ed01d0645689985f` triggered Agent Runtime Check run 35481415797, which reached the validator and failed before target work.

The processing state at that exact report commit carries `heartbeat.activity_kind: "production_start"`. The authoritative liveness contract and runtime validator permit the production startup state as `starting`; `production_start` is not an allowed activity kind. Earlier valid production claims use `starting` for the same lease-claim phase. Therefore this is a concrete control-plane producer/transition defect, not a report-format or product-path failure.

The pending independent OTK review for shift 79 remains authoritative and must complete before the next production shift. The product-side snapshot-preservation commit and exact Windows E2E failure evidence remain untouched.

## Decision

Repair the control-plane transition that emits `heartbeat.activity_kind` for a production lease claim so it emits canonical `starting`, without expanding the validator allow-list and without weakening Reporting v2, heartbeat, lease, fencing, immutable-report or OTK gates.

The next eligible production cycle must first complete the pending shift-79 OTK review, then verify a normal production claim/start-report path through unchanged Agent Runtime Check. Only terminal SUCCESS may unlock target-repository work. After that proof, resume the preserved boot-debugging continuation by consuming exact Windows E2E run 35480398951 failure evidence.

No owner decision, transfer, scheduler mutation, or Definition-of-Done change is required.
