# DIR-021 — CANONICAL REPORT PREFLIGHT THEN DETERMINISTIC CAS MUTATION

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-049
supersedes: DIR-020

## Direction

1. Before immutable publication, construct the Reporting v2 start report from the exact canonical format in `.agent/reporting.md`. The literal protocol labels must appear exactly: `Проект:`, `Работник:`, `Смена:`, `Начало смены:`, `СТАРТОВЫЙ ДОКЛАД:`, `ОЦЕНКА ПРЕДШЕСТВЕННИКА:`, `МОЙ ПЛАН:`.
2. Validate the complete candidate against the shared report contract before creating `.agent/reports/starts/...`. Do not publish an approximate or hand-reformatted variant.
3. After immutable creation, re-read the exact file and wait for that exact report commit's `Agent Runtime Check` terminal SUCCESS. Any failure freezes target work and is persisted as a control-plane blocker; never rewrite the immutable report.
4. After SUCCESS, do no architecture/API rediscovery. Fetch the complete current UTF-8 `tools/ios-ramdisk-tool/main.go` from `lvlaksim1/iOS-Research-Runtime` `main` and retain its exact blob SHA.
5. Apply only the already-localized snapshot Name/ModTime preservation edit in memory. Require the expected old fragment to match exactly once.
6. Submit the complete modified file with GitHub Contents `update_file` using that exact blob SHA as CAS guard. Whole-file transport is not permission for unrelated formatting, import, writer, workflow or architecture changes.
7. Immediately persist a checkpoint containing the returned target commit SHA and state that Name/ModTime snapshot preservation is durable.
8. Then continue focused tests -> required Windows gate -> exact Windows E2E in the same live shift when evidence is available.
9. If complete-file retrieval is truncated/unavailable, CAS rejects, or the expected old fragment does not match exactly once, make no target mutation and persist the exact blocker.
10. Keep APFS writer semantics frozen outside this bounded snapshot-preservation mutation.

## Success criterion

A canonical Reporting v2 start report that passes preflight and exact-commit Runtime Check, followed by a committed bounded Name/ModTime snapshot-preservation mutation and immediate checkpoint of its exact target commit SHA; thereafter focused tests, Windows gate and exact Windows E2E without weakening the object Definition of Done.