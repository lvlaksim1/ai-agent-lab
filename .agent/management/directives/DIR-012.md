# DIR-012 — IMPLEMENT FIRST, EVIDENCE NEXT

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-025
supersedes: DIR-011

## Direction

Preserve DIR-011's evidence-first APFS rule, but change shift execution discipline.

After the pending OTK review is fully persisted, the next production shift must start from the already-verified facts: decoded source/rebuilt NXSB readers and tested `writeNXEvidence` serializer exist, and the remaining Go change is bounded wiring in `main.go`.

Do not spend another shift re-surveying that architecture unless current repository state directly contradicts those facts.

1. Make and persist the smallest safe source/rebuilt NXSB wiring edit early in the shift.
2. Persist an intermediate checkpoint immediately after the target edit and refresh heartbeat.
3. Replace/remove the known wrong-layer C# raw-DMG pre-provision abort only as required to let the decoded evidence path run.
4. Run/consume the mandatory Windows gates and exact E2E.
5. Identify the first causally relevant structural mismatch before changing any APFS writer field.

## Constraints

- Never weaken or bypass proof gates or Definition of Done.
- Do not guess APFS semantics.
- APFS writer remains frozen until causal evidence exists.
- Do not repeat already-settled architecture investigation merely because a previous runtime was lost.
- If a prerequisite is contradicted by current code, persist that contradiction as evidence and adapt minimally.
- Runtime loss must not erase completed target work: checkpoint immediately after the bounded edit.