# DIR-025 — RECOVER REPORTING V2 START GATE, THEN CONTINUE DIR-024

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-061
supersedes: DIR-024

## Direction

1. Treat shift 98 as a control-plane/report-format failure, not technical evidence against DIR-024. Do not modify the immutable shift-98 start report.
2. Before publishing the next immutable start report, render it using the canonical Reporting v2 contract (`tools/agent-report-contract.mjs` / `.github/scripts/lib/report-contract.cjs`) so every required literal marker is exact, including `ОЦЕНКА ПРЕДШЕСТВЕННИКА:` and `МОЙ ПЛАН:`.
3. Validate the complete candidate with the same Reporting v2 validator before publication. If candidate validation is not clean, do not publish it and do not touch the target repository; persist the concrete control-plane defect instead.
4. After publication, require terminal SUCCESS from the exact `Agent Runtime Check` run for that report commit before substantive target work.
5. Once the gate is green, resume the technical substance of DIR-024 unchanged: prove the smallest typed APSB LastModTime assignment point and normal checksum/write path before any target mutation.
6. If proven, apply only the bounded LastModTime preservation mutation with authoritative preimage and SHA/CAS, immediately checkpoint exact target SHA, then run focused tests -> Windows gate -> exact Windows E2E and consume terminal evidence.
7. Keep XID/checkpoint semantics and `metaCryptoKeyOsVersion` frozen absent new discriminating evidence.

## Success criterion

The next immutable Reporting v2 start report passes its exact Agent Runtime Check, after which the checksum-safe APSB LastModTime mechanism proof proceeds without weakening DIR-024 or mutating target before the gate.
