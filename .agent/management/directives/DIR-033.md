# DIR-033 — RESTORE SHIFT-148 REPORTING V2 PRODUCER PATH

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-084
temporarily_supersedes: DIR-029

## Direction

1. Treat shift 148 as the authoritative failing case: immutable start report `.agent/reports/starts/shift-148-borisych-ios-runtime-release-20260918-032.md`, binding commit `7d5d2c9aec7263c7847f03eba8b0895baf6770ab`, and Agent Runtime Check `35620263457` failing at `Validate agent runtime invariants`.
2. Determine why the shift-148 report omitted the canonical Reporting v2 literal metadata/marker lines (`Проект:`, `Работник:`, `Смена:`, `Начало смены:` and canonical section markers) despite the previously repaired producer path. Compare the exact producer transition against the validator and the known-good canonical renderer path; do not guess and do not rewrite the immutable shift-148 report.
3. Repair only the demonstrated producer/validator/control-plane contract defect. Preserve scheduler immutability, single-worker fencing, GitHub time authority, lease/stale recovery, immutable Reporting v2 publication, OTK independence and all existing proof gates. Never weaken the validator merely to obtain green CI.
4. Prove recovery with an authoritative successful exact `Agent Runtime Check` on a newly produced canonical start-report path before any mutation or new CI launch in `lvlaksim1/iOS-Research-Runtime`.
5. Once the barrier is green, resume DIR-029 from the preserved engineering boundary: exact E2E extentref root-record comparison at target `a4897aa79186d0e492ca0e5f8fbdd0ad4338bcc9`, explaining the established source/rebuilt extentref `NumberOfKeys` divergence without repeating closed APFS/package hypotheses.

## Success criterion

The concrete shift-148 producer-path regression/bypass is identified and repaired without weakening any invariant; a newly produced canonical Reporting v2 start report passes exact Agent Runtime Check; then DIR-029 resumes from the preserved extentref root-record evidence boundary.