# DIR-026 — PRESERVE APSB METACRYPTOKEYOSVERSION

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-063
supersedes: DIR-025

## Direction

1. Treat shift-101 evidence as the authoritative discriminator: source `metaCryptoKeyOsVersion=407249186 (0x18462122)` versus rebuilt `0`; adjacent MetaCrypto fields and LastModTime already match.
2. Do not reopen LastModTime, XID/checkpoint, or unrelated MetaCrypto semantics without new discriminating evidence.
3. Prove the smallest typed reader/writer assignment point for MetaCryptoKeyOSVersion and the normal checksum/serialization path before target mutation.
4. Apply exactly one bounded source-preserving KeyOSVersion mutation using authoritative preimage and SHA/CAS. Do not synthesize or hard-code a value when the source value can be propagated through the existing builder/input path.
5. Immediately checkpoint the exact target SHA after mutation.
6. Run focused tests -> Windows gate -> exact Windows E2E and consume terminal evidence. Compare the rebuilt APSB field and boot outcome against the source/evidence baseline.
7. If the field is preserved but APFS mountroot error 79 remains, persist the resulting discriminating evidence and stop broadening the writer until a new causal difference is demonstrated.

## Success criterion

The rebuilt APSB preserves the source MetaCryptoKeyOSVersion through the normal checksum/serialization path, verification gates are consumed to terminal state, and exact Windows E2E determines whether this discriminator removes or advances the APFS mountroot failure.
