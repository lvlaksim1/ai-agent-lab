# DIR-027 — LOSSLESS PREIMAGE THEN BOUNDED KEYOSVERSION REPAIR

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-064
supersedes: DIR-026

## Direction

1. Do not repeat broad APFS investigation. Treat shifts 102-103 as sufficient proof of the repair boundary: rebuilt APSB KeyOSVersion must preserve the source value; bounded fallback is volume-paddr resolution → uint32 offset 108 → Fletcher64 reseal over block[8:] → checksum validation → same-block write.
2. Before any target write, obtain a lossless authoritative preimage of the exact target file. If a whole-file connector read is truncated, reconstruct it deterministically from bounded, non-overlapping GitHub reads or an exact Git blob/raw route. Verify reconstructed bytes/content against the authoritative Git blob SHA before mutation. A truncated display is not a blocker while these routes remain available.
3. Re-read production fence immediately before target mutation. Apply exactly one bounded source-preserving KeyOSVersion repair. Do not modify LastModTime, XID/checkpoint, adjacent MetaCrypto fields, or unrelated code.
4. Use SHA/CAS for the target write and immediately checkpoint the exact target commit SHA.
5. Run focused tests → Windows gate → exact Windows E2E and consume terminal evidence in the same live shift. Pending CI is active evidence wait, not a handoff boundary.
6. If KeyOSVersion is preserved but mountroot error 79 remains, persist the new discriminating evidence and stop broadening the writer until another causal difference is demonstrated.

## Success criterion

The target mutation is based on a blob-verified lossless preimage, rebuilt APSB preserves source MetaCryptoKeyOSVersion with valid checksum, and terminal focused/Windows/E2E evidence establishes the resulting boot outcome.
