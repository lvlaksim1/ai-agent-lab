# DIR-020 — DETERMINISTIC WHOLE-FILE CAS SNAPSHOT MUTATION

object_id: ios-research-runtime
effective_from: NEXT_SHIFT
status: ACTIVE
source_decision: DEC-048
supersedes: DIR-019

## Direction

1. Preserve the Reporting v2 barrier: publish one canonical immutable start report and wait for the exact report commit's Agent Runtime Check terminal SUCCESS before any target mutation.
2. After SUCCESS, do no architecture/API rediscovery. Fetch the complete current UTF-8 contents of `tools/ios-ramdisk-tool/main.go` from `lvlaksim1/iOS-Research-Runtime` `main` and retain its exact blob SHA.
3. Apply only the already-localized snapshot Name/ModTime preservation edit in memory. Require the expected old fragment to match exactly once. Whole-file transport is not permission for any unrelated formatting, import, writer, workflow, or architecture change.
4. Submit the complete modified file with GitHub Contents `update_file` using the exact fetched blob SHA as CAS guard. Do not search for alternate write mechanisms.
5. Immediately persist a checkpoint containing the returned target commit SHA and the statement that Name/ModTime snapshot preservation is durable. Only then continue further investigation/tests.
6. If complete-file retrieval is truncated/unavailable, CAS rejects, or the expected old fragment does not match exactly once, make no target mutation; persist the exact failure as blocker for management.
7. After durable checkpoint, continue focused tests -> required Windows gate -> exact Windows E2E in the same live shift when evidence is available.
8. Keep APFS writer semantics frozen outside this bounded snapshot-preservation mutation. Broader writer changes require new causal structural evidence.

## Success criterion

A committed bounded Name/ModTime snapshot-preservation mutation plus an immediate persisted checkpoint with its exact target commit SHA; thereafter focused tests, Windows gate and exact Windows E2E without weakening the object Definition of Done.